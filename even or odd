#include<stdio.h>
int main(void){
   int num;
  printf("enter a number:");
  scanf("%d",&num);

  switch(num%2)
    {
      case 0:
      printf("given no is even");
      break;

      case 1:
      printf("given no is odd");
        break;
      
    }
  return 0;
  
}
